# Config

The `sculptor.json` file controls the main framework behavior.

Common keys:

- `project.srcRoot`
- `project.entryFile`
- `project.devServer`
- `routing.style`
- `testing.generate`
- `testing.framework`
- `frameworkLock`
- `logging.enabled`
- `logging.dogMode`
