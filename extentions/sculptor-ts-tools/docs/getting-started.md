# Getting Started

If your workspace contains `sculptor.json`, SculptorTS Tools activates automatically.

The extension will:

- show `⚒ Sculptor Project` in the status bar
- expose `sculptor.isProject` to menus and commands
- validate `sculptor.json` and `props.json`
- provide a docs sidebar in the activity bar
