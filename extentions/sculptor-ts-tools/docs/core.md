# Core

The Sculptor core package owns the runtime bootstrap, request context, package composition, and decorator utilities.

The extension uses these docs to explain:

- `@Package`
- `@Controller`
- `@Service`
- `@Repository`
- `@AutoInject`
- `req.ctx`
