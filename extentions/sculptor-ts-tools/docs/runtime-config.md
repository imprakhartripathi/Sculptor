# Runtime Config

The `props.json` file controls runtime properties for the app.

Supported values:

- `app.port`
- `app.prefix`

This view is shown when `Explain Current File` is used on `props.json`.
